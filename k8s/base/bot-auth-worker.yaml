apiVersion: apps/v1
kind: Deployment
metadata:
  name: bot-auth-worker
  namespace: hivagold
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bot-auth-worker
  template:
    metadata:
      labels:
        app: bot-auth-worker
    spec:
      containers:
        - name: bot-auth-worker
          image: bot-auth-worker:1.0.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8002
              name: http
          envFrom:
            - configMapRef:
                name: hivagold-config
          env:
            - name: AUTH_WORKER_HOST
              value: 0.0.0.0
            - name: AUTH_WORKER_PORT
              value: "8002"
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: bot-auth-worker
  namespace: hivagold
spec:
  selector:
    app: bot-auth-worker
  ports:
    - port: 8002
      targetPort: http
      name: http
