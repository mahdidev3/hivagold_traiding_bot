apiVersion: apps/v1
kind: Deployment
metadata:
  name: bot-room-worker
  namespace: hivagold
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bot-room-worker
  template:
    metadata:
      labels:
        app: bot-room-worker
    spec:
      containers:
        - name: bot-room-worker
          image: bot-room-worker:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8005
              name: http
          envFrom:
            - configMapRef:
                name: hivagold-config
          env:
            - name: ROOM_WORKER_HOST
              value: 0.0.0.0
            - name: ROOM_WORKER_PORT
              value: "8005"
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: hivagold-secrets
                  key: REDIS_PASSWORD
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: bot-room-worker
  namespace: hivagold
spec:
  selector:
    app: bot-room-worker
  ports:
    - port: 8005
      targetPort: http
      name: http
